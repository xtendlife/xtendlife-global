{% comment %}
  Media Hero Slider (image/video per slide)
  - No <script> here. Assumes your global Swiper init reads data-* attributes.
  - Dot pagination overlaps bottom-right inside the media.
{% endcomment %}

{{ 'section-image-banner.css' | asset_url | stylesheet_tag }}

{%- style -%}

#MediaHero-{{ section.id }} .banner { position: relative; overflow: hidden; }
#MediaHero-{{ section.id }} .banner__box { background: transparent; }
#MediaHero-{{ section.id }} .banner__content { position: relative; z-index: 2; }
#MediaHero-{{ section.id }} .banner__media { position: relative; z-index: 1; }
#MediaHero-{{ section.id }} .banner--small .banner__media,
#MediaHero-{{ section.id }} .banner--medium .banner__media,
#MediaHero-{{ section.id }} .banner--large .banner__media {
  position: absolute; inset: 0; height: 100%;
}
#MediaHero-{{ section.id }} .banner__media img.media-hero__img,
#MediaHero-{{ section.id }} .banner__media video {
  position: absolute; inset: 0;
  width: 100%; height: 100%;
  object-fit: cover; object-position: center;
  display: block;
}
/* Small text base */
#MediaHero-{{ section.id }} .banner__smalltext {
  margin-top: 2000wrem;
  line-height: 1.2;
}

/* #MediaHero-{{ section.id }} .button--secondary,
#MediaHero-{{ section.id }} .button--tertiary {
  background-color: #fff;
  color: #1F1946;
} */

/* Desktop */
@media (min-width: 750px) {
  #MediaHero-{{ section.id }} .banner--large {
    min-height: calc(100vh - 83px);
    align-items: center;
  }
  #MediaHero-{{ section.id }} .banner__content { padding-top: 125px; }
  #MediaHero-{{ section.id }} .banner__heading { line-height: 1; }
  #MediaHero-{{ section.id }} .banner--stacked .banner--content-align-left { margin: auto; }
  #MediaHero-{{ section.id }} .banner--stacked .subtitle { max-width: 30vw; }
  #MediaHero-{{ section.id }} .subtitle.banner__text {
    font-size: 24px;
  }
  #MediaHero-{{ section.id }} .mhs-mobile-inline-img {
    display: none;
  }
}

/* Mobile */
@media (max-width: 749px) {
  #MediaHero-{{ section.id }} .banner::after { opacity: 0.15 !important; }
  #MediaHero-{{ section.id }} .banner__content { height: 100%; align-items: flex-end; }
  #MediaHero-{{ section.id }} .banner__box { padding-bottom: 6rem; text-align: left; width: calc(100vw - 5rem); }
  #MediaHero-{{ section.id }} .banner__heading { font-size: 28px; line-height: 1.2; margin: 0; max-width: 300px !important; }
  #MediaHero-{{ section.id }} .subtitle.banner__text {
    font-size: 18px;
    margin: 2rem 0 0;
    max-width: 250px !important;
  }

  #MediaHero-{{ section.id }} .banner__buttons { justify-content: flex-start !important; margin-top: 3rem; width: calc(100% - 0.5rem); }
  #MediaHero-{{ section.id }} .button { width: 100%; font-size: 1.6rem !important; }
  #MediaHero-{{ section.id }} .hero-pagination.swiper-pagination { padding-right: 3rem !important; }
  #MediaHero-{{ section.id }} .banner--stacked .banner__box { display: none; }
  #MediaHero-{{ section.id }} .mhs-stacked-mobile { padding: 4rem 2rem; }
  #MediaHero-{{ section.id }} .mhs-stacked-mobile h2 { max-width: calc(100% - 2rem); margin: 0; font-size: 2.8rem; }
  #MediaHero-{{ section.id }} .mhs-stacked-mobile p { max-width: calc(100% - 2rem); margin: 1.5rem 0; font-size: 1.6rem; }

  #MediaHero-{{ section.id }} .mhs-mobile-inline-img {
    display: block;
    width: calc(100% - 2rem);
    height: auto;
    margin-bottom: 2rem;
  }
  /* If inline mobile image is used, hide all background media */
  #MediaHero-{{ section.id }} .banner.has-mobile-inline-image .banner__media {
    display: none !important;
  }
  /* Remove overlay too */
  #MediaHero-{{ section.id }} .banner.has-mobile-inline-image::after {
    display: none !important;
  }
  /* Apply custom background when inline mobile image is active */
  #MediaHero-{{ section.id }} .banner.has-mobile-inline-image {
    background-color: var(--mobile-inline-bg, transparent);
  }
  /* Ensure content box doesn’t override it */
  #MediaHero-{{ section.id }} .banner.has-mobile-inline-image .banner__box {
    background: transparent;
  }
}

/* Full-bleed helper & pagination */
#MediaHero-{{ section.id }} .is-fullbleed { width: 100vw; }
.section-{{ section.id }}-padding { overflow-x: clip; }
#MediaHero-{{ section.id }} .hero-pagination.swiper-pagination{
  position: absolute; right: 0; bottom: 25px; padding-right: 2rem; display: flex; justify-content: flex-end;
}

/* Overlay per slide */
#MediaHero-{{ section.id }} .banner::after{
  content:""; position:absolute; inset:0; background:#000;
  opacity: {{ section.settings.image_overlay_opacity | divided_by: 100.0 }};
  pointer-events:none; z-index: 1;
}

/* Dots */
#MediaHero-{{ section.id }} .hero-pagination .swiper-pagination-bullet {
  width: 10px; height: 10px; border-radius: 999px; background: #fff; opacity: .4;
}
#MediaHero-{{ section.id }} .hero-pagination .swiper-pagination-bullet-active {
  background: #fff; opacity: 1; width: 46px;
}

{%- endstyle -%}



{%- liquid
  assign fetch_priority = 'auto'
  if section.index == 1
    assign fetch_priority = 'high'
  endif
-%}

{% if section.settings.wide_container %}<div class="nav__offset_bg color-{{ section.settings.color_scheme }}"><div class="page-width--wide">{% endif %}

<section id="MediaHero-{{ section.id }}" class="section section-{{ section.id }}-padding"
         data-swiper-root>
  <div 
    class="swiper ts2-swiper hero-swiper is-fullbleed" 
    data-swiper 
    data-mode="fixed" 
    data-gap="{{ section.settings.gap | default: 0 }}" 
    data-speed="{{ section.settings.speed | default: 500 }}" 
    data-autoplay="{{ section.settings.autoplay | default: false }}" 
    data-autoplay-delay="{{ section.settings.autoplay_delay | default: 4000 }}" 
    data-dots="true" 
    data-pagination=".hero-pagination" 
  >

    <div class="swiper-wrapper">
      {% for block in section.blocks %}
        {% assign b = block.settings %}
        {% liquid
          assign sizes = '100vw'
          assign widths = '375, 550, 750, 1100, 1500, 1780, 2000, 3000, 3840'
          assign adapt_pad = ''
          if section.settings.image_height == 'adapt'
            if b.image != blank
              assign adapt_pad = 1 | divided_by: b.image.aspect_ratio | times: 100 | append: '%'
            else
              assign adapt_pad = b.video_adapt_ratio | default: 56.25 | append: '%'
            endif
          endif
          assign fetch_priority = 'auto'
          if section.index == 1
            assign fetch_priority = 'high'
          endif
        %}
        <style>
          @media (min-width: 750px) {
            .swiper-slide[data-slide-id="slide-{{ block.id }}"] .subtitle.banner__text {
              font-size: {{ b.subtitle_font_size_desktop | default: 24 }}px !important;
            }
            .swiper-slide[data-slide-id="slide-{{ block.id }}"] .banner__smalltext {
              font-size: {{ b.small_text_font_size_desktop | default: 12 }}px !important;
            }
          }
          @media (max-width: 749px) {
            .swiper-slide[data-slide-id="slide-{{ block.id }}"] .subtitle.banner__text {
              font-size: {{ b.subtitle_font_size_mobile | default: 18 }}px !important;
            }
            .swiper-slide[data-slide-id="slide-{{ block.id }}"] .banner__smalltext {
              font-size: {{ b.small_text_font_size_mobile | default: 10 }}px !important;
            }
          }
          {% if b.text_color != blank %}
            @media (min-width: 750px) {
              .swiper-slide[data-slide-id="slide-{{ block.id }}"] .banner__heading.inline-richtext {
                color: {{ b.text_color }} !important;
              }
              .swiper-slide[data-slide-id="slide-{{ block.id }}"] .subtitle.banner__text {
                color: {{ b.text_color }} !important;
              }
              .swiper-slide[data-slide-id="slide-{{ block.id }}"] .banner__smalltext {
                color: {{ b.text_color }} !important;
              }
            }
          {% endif %}
          {% if b.text_color_mobile != blank %}
            @media (max-width: 749px) {
              .swiper-slide[data-slide-id="slide-{{ block.id }}"] .banner__heading.inline-richtext {
                color: {{ b.text_color_mobile }} !important;
              }
              .swiper-slide[data-slide-id="slide-{{ block.id }}"] .subtitle.banner__text {
                color: {{ b.text_color_mobile }} !important;
              }
              .swiper-slide[data-slide-id="slide-{{ block.id }}"] .banner__smalltext {
                color: {{ b.text_color_mobile }} !important;
              }
            }
          {% endif %}
        </style>
        <div class="swiper-slide" data-slide-id="slide-{{ block.id }}" {{ block.shopify_attributes }}>

          <div
            class="
              banner
              banner--{{ section.settings.image_height }}
              {% if section.settings.show_text_box == false %}banner--desktop-transparent{% endif %}
              {% if section.settings.stack_text_on_mobile %}banner--stacked{% endif %}
              {% if b.mobile_inline_image != blank %}has-mobile-inline-image{% endif %}
              color-{{ section.settings.color_scheme }}
            "
            {% if b.mobile_inline_image != blank and b.mobile_inline_bg != blank %}
              style="--mobile-inline-bg: {{ b.mobile_inline_bg }};"
            {% endif %}
          >
            <div class="banner__media media">
              {% if section.settings.image_height == 'adapt' %}
                <div style="padding-bottom: {{ adapt_pad }};"></div>
              {% endif %}
              {% if b.video_mp4 != blank %}
                <video autoplay muted loop playsinline preload="metadata"
                  {% if b.video_poster != blank %}poster="{{ b.video_poster | image_url: width: 1920 }}"{% endif %}>
                  <source src="{{ b.video_mp4 | escape }}" type="video/mp4">
                </video>
              {% elsif b.image != blank %}
                <picture>
                  {% if b.image_mobile != blank %}
                    <source
                      media="(max-width: 749px)"
                      srcset="{{ b.image_mobile | image_url: width: 1100 }}">
                  {% endif %}
                  {% if b.image_tablet != blank %}
                    <source
                      media="(min-width: 750px) and (max-width: 990px)"
                      srcset="{{ b.image_tablet | image_url: width: 1500 }}">
                  {% endif %}
                  <img
                    src="{{ b.image | image_url: width: 3840 }}"
                    alt="{{ b.image.alt | escape }}"
                    width="{{ b.image.width }}"
                    height="{{ b.image.height }}"
                    sizes="{{ sizes }}"
                    srcset="
                      {{ b.image | image_url: width: 750 }} 750w,
                      {{ b.image | image_url: width: 1100 }} 1100w,
                      {{ b.image | image_url: width: 1500 }} 1500w,
                      {{ b.image | image_url: width: 2000 }} 2000w,
                      {{ b.image | image_url: width: 3000 }} 3000w,
                      {{ b.image | image_url: width: 3840 }} 3840w
                    "
                    fetchpriority="{{ fetch_priority }}"
                    class="media-hero__img"
                  >
                </picture>
              {% else %}
                {{ 'lifestyle-1' | placeholder_svg_tag: 'placeholder-svg' }}
              {% endif %}
            </div>

            <div class="banner--content-align-{{ section.settings.desktop_content_alignment }} banner--content-align-mobile-{{ section.settings.mobile_content_alignment }}">
              <div class="banner__content banner__content--{{ section.settings.desktop_content_position }} page-width">
                <div class="banner__box content-container content-container--full-width-mobile gradient">
                  {% if b.mobile_inline_image != blank %}
                    <img 
                      class="mhs-mobile-inline-img" 
                      src="{{ b.mobile_inline_image | image_url: width: 600 }}"
                      alt="{{ b.mobile_inline_image.alt | escape }}"
                      loading="lazy"
                    >
                  {% endif %}

                  {% if b.heading != blank %}
                    <h2
                      class="banner__heading inline-richtext {{ b.heading_size }}"
                      style="
                        {% if b.heading_max_width %}max-width: {{ b.heading_max_width }}px;{% endif %}
                      "
                    >{{ b.heading }}</h2>
                  {% endif %}

                  {% if b.text != blank %}
                    <p
                      class="subtitle banner__text {{ b.text_style }}"
                      style="
                        {% if b.text_max_width %}max-width: {{ b.text_max_width }}px;{% endif %}
                      "
                    >{{ b.text }}</p>
                  {% endif %}

                  {% if b.small_text != blank %}
                    <p
                      class="banner__smalltext"
                      style="
                        {% if b.text_max_width %}max-width: {{ b.text_max_width }}px;{% endif %}
                      "
                    >{{ b.small_text }}</p>
                  {% endif %}

                  {% if b.button_label_1 != blank or b.button_label_2 != blank %}
                    <div class="banner__buttons{% if b.button_label_1 != blank and b.button_label_2 != blank %} banner__buttons--multiple{% endif %}">
                      {% if b.button_label_1 != blank %}
                        <a {% if b.button_link_1 == blank %}role="link" aria-disabled="true"{% else %}href="{{ b.button_link_1 }}"{% endif %}
                           class="button{% if b.button_style_secondary_1 %} button--secondary{% else %} button--primary{% endif %}">{{ b.button_label_1 | escape }}</a>
                      {% endif %}
                      {% if b.button_label_2 != blank %}
                        <a {% if b.button_link_2 == blank %}role="link" aria-disabled="true"{% else %}href="{{ b.button_link_2 }}"{% endif %}
                           class="button{% if b.button_style_secondary_2 %} button--secondary{% else %} button--primary{% endif %}">{{ b.button_label_2 | escape }}</a>
                      {% endif %}
                    </div>
                  {% endif %}
                </div>
              </div>
            </div>

            {% if section.settings.stack_text_on_mobile and b.text != blank %}
              <div class="mhs-stacked-mobile banner__text rte {{ b.text_style }}">
                  {% if b.heading != blank %}
                    <h2 class="banner__heading inline-richtext {{ b.heading_size }}">{{ b.heading }}</h2>
                  {% endif %}
                  {% if b.text != blank %}
                    <div class="banner__text rte {{ b.text_style }}"><p class="subtitle">{{ b.text }}</p></div>
                  {% endif %}
              </div>
            {% endif %}

          </div>
        </div>
      {% endfor %}
    </div>
    <div class="page-width--wide" style="position: relative;">
        <div class="hero-pagination swiper-pagination"></div>
    </div>
  </div>
</section>


{% if section.settings.wide_container %}</div></div>{% endif %}

{% schema %}
{
  "name": "Video + Image Hero Slider",
  "tag": "section",
  "class": "section",
  "disabled_on": { "groups": ["header","footer"] },
  "settings": [
    { "type": "checkbox", "id": "wide_container", "default": false, "label": "Enable wide container" },
    {
      "type": "range", "id": "image_overlay_opacity",
      "min": 0, "max": 100, "step": 10, "unit": "%",
      "label": "Overlay opacity", "default": 0
    },
    {
      "type": "select", "id": "image_height", "label": "Banner height",
      "options": [
        { "value": "adapt", "label": "Adapt to media" },
        { "value": "small", "label": "Small" },
        { "value": "medium", "label": "Medium" },
        { "value": "large", "label": "Large" }
      ],
      "default": "medium"
    },
    {
      "type": "select", "id": "desktop_content_position", "label": "Desktop content position",
      "options": [
        { "value": "top-left", "label": "Top left" },
        { "value": "top-center", "label": "Top center" },
        { "value": "top-right", "label": "Top right" },
        { "value": "middle-left", "label": "Middle left" },
        { "value": "middle-center", "label": "Middle center" },
        { "value": "middle-right", "label": "Middle right" },
        { "value": "bottom-left", "label": "Bottom left" },
        { "value": "bottom-center", "label": "Bottom center" },
        { "value": "bottom-right", "label": "Bottom right" }
      ],
      "default": "middle-center"
    },
    {
      "type": "select", "id": "desktop_content_alignment", "label": "Desktop content alignment",
      "options": [
        { "value": "left", "label": "Left" },
        { "value": "center", "label": "Center" },
        { "value": "right", "label": "Right" }
      ],
      "default": "center"
    },
    { "type": "checkbox", "id": "show_text_box", "default": true, "label": "Show text box" },
    { "type": "color_scheme", "id": "color_scheme", "label": "Color scheme", "default": "scheme-1" },
    { "type": "checkbox", "id": "stack_text_on_mobile", "default": true, "label": "Show text below media on mobile" },

    { "type": "header", "content": "Slider behavior (read by your global Swiper init)" },
    { "type": "checkbox", "id": "autoplay", "label": "Autoplay", "default": true },
    { "type": "range", "id": "autoplay_delay", "label": "Autoplay delay (ms)", "min": 1500, "max": 8000, "step": 100, "default": 4000 },
    { "type": "checkbox", "id": "loop", "label": "Loop slides", "default": true },
    { "type": "range", "id": "speed", "label": "Transition speed (ms)", "min": 150, "max": 1200, "step": 50, "default": 500 }
  ],
  "blocks": [
    {
      "type": "slide",
      "name": "Slide",
      "settings": [
        { "type": "image_picker", "id": "image", "label": "Image (desktop, fallback)" },
        { "type": "image_picker", "id": "image_tablet", "label": "Tablet image (750px–990px)" },
        { "type": "image_picker", "id": "image_mobile", "label": "Mobile image (<750px)" },
        { "type": "image_picker", "id": "mobile_inline_image", "label": "Inline mobile image (above title, mobile only)" },
        { "type": "color", "id": "mobile_inline_bg", "label": "Mobile inline background color", "default": "#ffffff" },
        { "type": "image_picker", "id": "video_poster", "label": "Video poster (optional)" },
        { "type": "url", "id": "video_mp4", "label": "Hosted MP4 URL (takes priority if set)" },
        { "type": "range", "id": "video_adapt_ratio", "label": "Adapt height ratio for video (%)", "min": 30, "max": 100, "step": 1, "default": 57, "info": "Used only when Banner height is 'Adapt' and slide uses video." },
        { "type": "color", "id": "text_color", "label": "Text color override (optional)" },
        { "type": "color", "id": "text_color_mobile", "label": "Text color override mobile (optional)" },
        { "type": "header", "content": "Heading" },
        { "type": "text", "id": "heading", "label": "Heading", "default": "Your headline here" },
        {
          "type": "select", "id": "heading_size", "label": "Heading size",
          "options": [
            { "value": "h2", "label": "H2" },
            { "value": "h1", "label": "H1" },
            { "value": "h0", "label": "H0" },
            { "value": "hxl", "label": "HXL" },
            { "value": "hxxl", "label": "HXXL" }
          ],
          "default": "h1"
        },
        { "type": "range", "id": "heading_max_width", "label": "Heading max-width (px)", "min": 200, "max": 1200, "step": 10, "default": 500, "info": "0 uses theme default" },
        { "type": "header", "content": "Text" },
        { "type": "inline_richtext", "id": "text", "label": "Text", "default": "Add supporting copy here." },
        {
          "type": "select", "id": "text_style", "label": "Text style",
          "options": [
            { "value": "body", "label": "Body" },
            { "value": "subtitle", "label": "Subtitle" },
            { "value": "caption-with-letter-spacing", "label": "Caption (spaced)" }
          ],
          "default": "body"
        },
        {
          "type": "range",
          "id": "subtitle_font_size_desktop",
          "label": "Subtitle font size — desktop (px)",
          "min": 6, "max": 32, "step": 1,
          "default": 24
        },
        {
          "type": "range",
          "id": "subtitle_font_size_mobile",
          "label": "Subtitle font size — mobile (px)",
          "min": 6, "max": 32, "step": 1,
          "default": 18
        },
        { "type": "range", "id": "text_max_width", "label": "Text max-width (px)", "min": 200, "max": 1200, "step": 10, "default": 350, "info": "0 uses theme default" },
        { "type": "header", "content": "Small Text (Optional)" },
        { "type": "inline_richtext", "id": "small_text", "label": "Text" },
        {
          "type": "range",
          "id": "small_text_font_size_desktop",
          "label": "Small text font size — desktop (px)",
          "min": 6, "max": 18, "step": 1,
          "default": 12
        },
        {
          "type": "range",
          "id": "small_text_font_size_mobile",
          "label": "Small text font size — mobile (px)",
          "min": 6, "max": 18, "step": 1,
          "default": 10
        },
        { "type": "header", "content": "Primary button" },
        { "type": "text", "id": "button_label_1", "label": "Label", "default": "Shop now" },
        { "type": "url", "id": "button_link_1", "label": "Link" },
        { "type": "checkbox", "id": "button_style_secondary_1", "label": "Use secondary style", "default": false },

        { "type": "header", "content": "Secondary button" },
        { "type": "text", "id": "button_label_2", "label": "Label" },
        { "type": "url", "id": "button_link_2", "label": "Link" },
        { "type": "checkbox", "id": "button_style_secondary_2", "label": "Use secondary style", "default": false }
      ]
    }
  ],
  "max_blocks": 10,
  "presets": [
    { 
        "name": "Video + Image Hero Slider", 
        "blocks": [ { "type": "slide" }, { "type": "slide" } ] }
  ]
}
{% endschema %}
